{
  "$comment": [
    "Without this, `yindee context` answers almost every question in this repo with the nine",
    "files of the cli/ workspace — it did so for the Card task and again for the components",
    "task. The site is the bulk of the code but the CLI is the only thing that looks like a",
    "package, so the default area detection ranks it first. These globs put the component,",
    "docs and style trees where they belong."
  ],
  "areas": {
    "frontend": [
      "src/components/**",
      "src/pages/**",
      "src/hooks/**",
      "src/lib/**",
      "src/styles/**"
    ],
    "docs": ["src/content/docs/**", "docs/**", "*.md"],
    "registry": ["registry.json", "scripts/build-registry.mjs", "public/r/**"],
    "cli": ["cli/**"]
  },
  "sensitive": ["src/styles/tokens.css", ".github/workflows/**", "package-lock.json"]
}
